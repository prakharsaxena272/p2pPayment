server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/transactiondb
    username: user
    password: pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

kafka:
  bootstrap-servers: localhost:9092
